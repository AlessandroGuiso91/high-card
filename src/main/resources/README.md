**Demo keys — DO NOT use in production.**

Generated for the high-card assessment. In a real deployment, RSA keys are
provisioned via a secrets manager (KMS, Vault, mounted file) and never
committed to version control. Rotate immediately if these were ever exposed
in a real environment.